apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "victoria-deals.fullname" . }}-test-connection"
  labels:
    {{- include "victoria-deals.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "victoria-deals.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
